import React from 'react';
import OverseasShippingForm from '../../components/OverseasShippingForm';

interface Props {}

const OverseasShipping: React.FC<Props> = (props) => <OverseasShippingForm />;

export default OverseasShipping;

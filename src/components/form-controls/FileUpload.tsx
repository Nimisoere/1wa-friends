import React from "react";

interface Props {
  title: string;
  description: string;
}

const FileUpload: React.FC<Props> = ({ title, description }) => {
  return <div></div>;
};

export default FileUpload;
